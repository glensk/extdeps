"""Internal, non-operational tooling vendored into this repo.

`external_deps.py` is the external-dependency registry/resolver — see
`42-Git/README_INTERDEPENDENCIES.md`. Edit only its `EXTERNAL_DEPS` block.
"""
