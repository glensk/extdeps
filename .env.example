# extdeps is a pure-stdlib library and reads no environment variables itself.
#
# The `<DEP>_BIN` / `<DEP>_ROOT` / `<DEP>_PATH` override variables belong to the
# CONSUMING repos: each consumer documents its own override vars in its own
# .env.example, next to its EXTERNAL_DEPS registry.
